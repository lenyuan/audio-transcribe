{
  "name": "lenyuan-monorepo",
  "private": true,
  "workspaces": [
    "packages/frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  },
  "dependencies": {
    "@google/genai": "^1.21.0",
    "@vercel/blob": "^0.25.0",
    "@vercel/node": "^3.2.20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}