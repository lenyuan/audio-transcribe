{
  "name": "lenyuan-monorepo",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}